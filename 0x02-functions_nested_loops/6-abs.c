#include "main.h"
/**
 *_abs - check main
 * @r: integer input
 * Description: This function returns absolute value
 * Return: the absolute  value of number r
 */
int _abs(int r)
{
	if (r >= 0)
	return (r);
	else
		return (r * -1);
}
